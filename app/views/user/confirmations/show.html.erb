<h2> You're almost done! Now create a password to securely access your account.</h2>
<%= simple_form_for(resource, :as => resource_name, :url => send(:"#{resource_name}_confirm_path"), html: {novalidate: true}) do |form| %>
  <%= form.input :password, :input_html => {:autofocus => true} %>
  <%= form.input :password_confirmation %>
  <%= form.input :confirmation_token, :as => :hidden %>
  <%= hidden_field_tag :frendly_token, params[:confirmation_token] %>
  <%= form.simple_fields_for :user_info do |i| %>
    <%= i.input :name %>
  <% end %>

  <%= form.button :submit , :label => 'Confirm Account' %>
<% end %>
